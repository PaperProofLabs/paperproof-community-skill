interface:
  display_name: "PaperProof Protocol"
  short_description: "Publish, query, and verify PaperProof artifacts through the protocol."
  default_prompt: "Use $paperproof-protocol to help me publish, update, query, or verify a PaperProof artifact on Sui and Walrus."

policy:
  allow_implicit_invocation: true
